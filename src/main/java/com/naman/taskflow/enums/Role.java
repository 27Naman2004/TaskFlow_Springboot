package com.naman.taskflow.enums;

/**
 * Role defines what "type" of user this is.
 * Right now we only have USER, but this makes it easy to add ADMIN later.
 */
public enum Role {
    USER,
    ADMIN
}
