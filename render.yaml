services:
  - type: web
    name: taskflow-api
    runtime: docker
    plan: free
    envVars:
      - key: PORT
        value: 8080
      - key: DATABASE_URL
        value: jdbc:h2:mem:taskflow_db;DB_CLOSE_DELAY=-1
      - key: DATABASE_DRIVER
        value: org.h2.Driver
